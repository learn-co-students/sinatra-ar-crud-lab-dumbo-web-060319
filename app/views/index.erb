<h1>Articles</h1>


<u1>
	<% @articles.all.each do |a| %>
	<li>
		<b> <%= a.id %> </b>
		<a href='<%= "articles/#{a.id}" %>'>
		<%= a.title %>
	</a>
	</li>
	<% end %>
</u1>

<h3><a href="articles/new">Create Article</a></h3>